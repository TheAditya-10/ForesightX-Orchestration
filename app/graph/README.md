# Orchestration Graph

This directory contains LangGraph state and workflow definitions.

## Purpose

The graph enforces controlled execution order so the orchestration layer behaves like a decision system, not an unstructured chatbot.

## Files

- `state.py`: typed workflow state
- `workflow.py`: node definitions and graph compilation
